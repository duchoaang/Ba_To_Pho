import Home from '@pages/Home';
import Documents from '@pages/Documents';
const routes = [
    {
        path: '/',
        component: Home,
    },
    {
        path: '/documents',
        component: Documents,
    },
];

export { routes };
