import Home from '@pages/Home';
import Upload from '@pages/Upload';

const routes = [
    {
        path: '/',
        component: Home,
    },
    {
        path: '/upload',
        component: Upload,
    },
];

export { routes };
